# wikiwright — the writer's brief

Generated by `wikiwright brief`. Do not edit: it is regenerated per install,
and the judgment that is not in it lives in the hand-written skill beside it.

Constitution digest: `1d5babb64915`

## The loop

1. `search` every name form, in both scripts, before you create anything.
2. `type show <type> --brief` — the contract, with live counts.
3. Draft the Markdown, then `write <path> --dry-run` and read the findings.
4. `write` for real; the engine stamps the dates and writes the History line.
5. Commit. The gate runs the same judge over what the commit would contain.

## Findings

If a finding has `fix`, run its `argv` (fill any placeholders first). If it has
`queue`, it is not yours — continue. A queued finding on a line you did not write
is a warning, not a block.

Of 73 passes, 14 name a fixer; the rest are queues or census rows.

One sentence on search: not-found is only as good as the coverage block. Never
claim absence while `caps.hit` is true — the cap cut the list before the end.

## Verbs

### `brief` — once per session: read this file

`wikiwright brief` — flags: --role <v>

```text
wikiwright brief --role writer
```

### `check` — the whole vault, and the generated artifacts

`wikiwright check` — flags: --write --limit <v> --rule <v> --path <v> --all --dry-run

```text
wikiwright check --root .
```

### `fix` — a finding that carries a `fix`: run its argv

`wikiwright fix` — flags: --rule <v> --path <v> --staged --line <v> --propose --expect <v> --dry-run

```text
wikiwright fix --rule sections --path wiki/parser.md --expect 1
```

### `graph` — coverage, derived: the edges a label carries, and the pages on one side that carry none

`wikiwright graph <subcommand>` — subcommands: edges — flags: --kind <v> --label <v> --inbound <v> --outbound <v> --missing --limit <v> --all

```text
wikiwright graph edges --label implements --label diverges-from --inbound requirement --missing
```

### `lint` — a draft, before it lands

`wikiwright lint` — flags: --page <v> --staged --stdin --since <v> --limit <v> --rule <v> --all --explain --path <v>

```text
wikiwright lint --root .
```

### `new` — a page that does not exist yet: the skeleton write

`wikiwright new <type> <title>` — flags: --dest <v> --set <v> --item <v> --date <v> --not-any-of <v> --dry-run

```text
wikiwright new architecture-overview "Architecture" --dest wiki/architecture.md
```

### `okf` — the interop conformance check, when a bundle claims the profile

`wikiwright okf <subcommand>` — subcommands: check

```text
wikiwright okf check
```

### `schema` — the whole generated registry, when a flag is in doubt

`wikiwright schema`

```text
wikiwright schema
```

### `search` — before creating anything: search both scripts

`wikiwright search [query]` — flags: --type <v> --tag <v> --title-contains <v> --limit <v> --near

```text
wikiwright search 张伟
```

### `type` — before writing: read the contract you are about to satisfy

`wikiwright type <subcommand> [name]` — subcommands: list, show — flags: --brief

```text
wikiwright type show subsystem
```

### `version` — which engine build is answering

`wikiwright version`

```text
wikiwright version
```

### `vocabulary` — when a category or label is in doubt: read what the vocabulary admits

`wikiwright vocabulary <subcommand> [name]` — subcommands: show — flags: --label <v> --target <v>

```text
wikiwright vocabulary show relations
```

### `write` — the write itself: Markdown in, one page out — or a directory of drafts landed together — judged before a byte lands

`wikiwright write [path]` — flags: --from <v> --section <v> --append --date <v> --replace-core <v> --retract <v> --correct <v> --core <v> --line <v> --coexist <v> --base <v> --not-any-of <v> --dry-run

```text
wikiwright write wiki/parser.md --dry-run
```

## Types

- `architecture-overview` — The engine's shape: the four layers and how a verdict flows through them; anchored to the entry points that define them. This bundle lives in the repository it documents, so origin is `.`.
- `charter` — Exactly one page — meta/charter.md: the bundle's intention and scope, loaded every session. Not the agent operating manual (CLAUDE.md / AGENTS.md).
- `code-concept` — A cross-cutting mechanism of the engine that lives in many places: an invariant, a law, a data shape spanning subsystems.
- `concept` — Explains what something is, why it works, or its current model.
- `decision` — A decision record of the engine, numbered D-nnn, citing the documents and pages that carry its evidence.
- `hub` — Gives a concise overview and curated routes.
- `integration` — An external system the engine talks to, and the contract with it; anchored to the code that speaks it.
- `ops-reference` — Lookup tables for the engine: the envelope and exit codes, environment variables, repository scripts; anchored to the code that defines them.
- `procedure` — Explains how to perform or verify an action.
- `quickstart` — Install, build, and verify the engine from a fresh clone; anchored to the files its commands come from.
- `reference` — Provides lookup-oriented facts, tables, commands, or records.
- `source-map` — Directory-to-purpose lookup for the wikiwright repository, anchored to the paths it lists.
- `subsystem` — One owned system of the engine, read at the pin and anchored to the directory it covers.
- `testing-guide` — How the engine's tests are organized, run, and written: the two runners, temp copies, the kit-code helper.

## Vocabularies, as declared

### `relations` (registered)

- `decided_by` — range: ["code/decision"]
- `mapped_in` — range: ["code/source-map"]
- `part_of` — range: ["code/subsystem","code/architecture-overview"]
- `verified_by` — range: ["code/testing-guide","code/quickstart"]

### `tags` (registered)

- `cli`
- `kernel`
- `kit`
- `meta`
- `repo`
- `stdlib`

## Vocabularies, as this vault uses them

### `categories`

No `categories` value has been observed in this vault yet.

### `relations`

`part_of` (25) · `mapped_in` (18) · `verified_by` (15) · `decided_by` (11)

### `sources`

No `sources` value has been observed in this vault yet.

### `tags`

`cli` (16) · `kernel` (14) · `meta` (7) · `repo` (4) · `stdlib` (3) · `kit` (2)

## What the loaded modules add

### Categories, by lifecycle class (`claims`)

A *supersede* category holds one truth: a later value replaces the earlier one and the engine writes the History line. An *accumulate* category collects dated observations: a later contrary one is appended, never a supersession. A *journal-only* category never becomes a standing fact.

### Reading code (`code`)

Never describe code you did not read at the pin. A statement about a file cites the file and the line as read at the page's `pin` (`packages/core/src/judge/index.ts:120`); a statement with no line is a guess, and a guess does not go on an anchored page.

### Anchoring (`code`)

A page that describes code carries `pin` (the commit it was read at), `origin` (`.` when the vault lives in the repository it documents, else the git URL) and `covers` (the repository-relative paths it describes; a subsystem page covers its directory). When `wikiwright freshness` names a page stale, re-read every covered path at the new head, correct the page, and re-pin; never advance a pin without the read.

### Relations (`code`)

`part_of` places a component in its subsystem or under the architecture overview; `mapped_in` names the source map that locates it; `verified_by` names the testing guide or quickstart that runs it; `decided_by` names the decision that shaped it. A relation that leaves an anchored page lands in History as a dated line quoting `label [[Target]]`.

### Decisions (`code`)

A decision record is never edited, only appended: the page is append-only, and a consequence learned later is a dated line at the end of Consequences. A reversed decision is a new record whose frontmatter `supersedes` the old one; `wikiwright retire` sets the successor. What a decision shaped is asked of the graph, not of the record: `wikiwright graph edges --label decided_by --inbound decision` lists the pages that name it.

## Names

Reserved basenames: `claude`, `agents`, `readme`, `skill`, `index`, `log`.

A wikilink resolves to a canonical basename, never to an alias: write
`[[Canonical|what you meant]]`. A page whose title is not Latin carries a Latin
alias, so both scripts find it.
